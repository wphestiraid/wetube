extends layouts/main

block content
  .form-container
    form(action=`/videos${routes.upload}`, method='post')
      label(for='file') Video File
      input(type='file', id='file', required=true, name='file')
      input(type='text', name='title', required=true, placeholder='Title')
      textarea(name='description', required=true, placeholder='Description')
      input(type='submit', value='Upload Video')